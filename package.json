{
    "name": "dependabot-security-checker",
    "version": "1.0.0",
    "description": "Check for open Dependabot alerts",
    "main": "dist/index.js",
    "scripts": {
      "build": "ncc build src/index.js -o dist",
      "test": "jest --passWithNoTests"
    },
    "dependencies": {
      "@actions/core": "^1.11.1",
      "@actions/github": "^6.0.1"
    },
    "devDependencies": {
      "@vercel/ncc": "^0.38.4",
      "jest": "^29.7.0"
    }
  }